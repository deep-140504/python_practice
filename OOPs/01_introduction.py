# every object has its own attributes
# every object has its own behaviour
# Object has something which can be used to store data
# functions in OOP is called as methods
# OOP is also about the way you think

# wahtever u do can be done using object, then why the class is important?
# explaination: class has design for its instances called objects, class has design for the instances being created
# classes are designed whereas the objects are the instance of the same
# classes are basically a blueprint